8상속
	상속하는 부모 클래스를 슈퍼 클래스(super class)	라 한다.
	상속받는 자식 클래스를 서브 클래스(sub class)	라 한다.
	자바에서는 다중 상속을 지원하지 않는다.
		하지만 인터페이스(interface)로 다중상속 가능.
	
	슈퍼 클래스의 protected 멤버 는 다음 두 가지 경우 접근 허용
		같은 패키지에 속한 모든 클래스
		상속되는 서브 클래스(패키지와는 상관 없음) 